<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    android:keepScreenOn="true">

    <androidx.media3.ui.PlayerView
        android:id="@+id/player_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:show_buffering="when_playing"
        app:use_controller="true"
        app:controller_layout_id="@layout/custom_player_controls" />

    <!-- This layout is for displaying info in the center of the screen (e.g., seek time) -->
    <LinearLayout
        android:id="@+id/info_layout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:visibility="gone"
        android:layout_marginBottom="84dp"
        tools:visibility="visible">

        <TextView
            android:id="@+id/info_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:shadowColor="#000000"
            android:shadowDx="1"
            android:shadowDy="1"
            android:shadowRadius="2"
            android:textSize="28sp"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            tools:text="22:30" />
    </LinearLayout>

    <!-- This is the vertical bar for the volume gesture indicator -->
    <LinearLayout
        android:id="@+id/volume_gesture_layout"
        android:layout_width="54dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical|start"
        android:layout_margin="16dp"
        android:background="#80000000"
        android:padding="8dp"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:visibility="gone"
        tools:visibility="visible">

        <TextView
            android:id="@+id/volume_progress_text"
            android:layout_width="wrap_content"
            android:layout_height="24dp"
            android:layout_marginVertical="8dp"
            android:gravity="center"
            android:textColor="@android:color/white"
            tools:text="58%" />

        <ProgressBar
            android:id="@+id/volume_progress_bar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="6dp"
            android:layout_height="125dp"
            android:progressDrawable="@drawable/vertical_progress_bar"
            tools:progress="58" />

        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginVertical="8dp"
            android:src="@android:drawable/ic_lock_silent_mode_off"
            tools:ignore="ContentDescription" />
    </LinearLayout>

    <!-- This is the vertical bar for the brightness gesture indicator -->
    <LinearLayout
        android:id="@+id/brightness_gesture_layout"
        android:layout_width="54dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical|end"
        android:layout_margin="16dp"
        android:background="#80000000"
        android:padding="8dp"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:visibility="gone"
        tools:visibility="visible">

        <TextView
            android:id="@+id/brightness_progress_text"
            android:layout_width="wrap_content"
            android:layout_height="24dp"
            android:layout_marginVertical="8dp"
            android:gravity="center"
            android:textColor="@android:color/white"
            tools:text="58%" />

        <ProgressBar
            android:id="@+id/brightness_progress_bar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="6dp"
            android:layout_height="125dp"
            android:progressDrawable="@drawable/vertical_progress_bar"
            tools:progress="58" />

        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginVertical="8dp"
            android:src="@android:drawable/ic_menu_day"
            tools:ignore="ContentDescription" />
    </LinearLayout>

</FrameLayout>